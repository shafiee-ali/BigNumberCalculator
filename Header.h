#include<string>
#include<cstdlib>
class calculator
{
public:
	std::string big_numbers_calculate(std::string);
	double normal_numbers_calculate(std::string);
	double fact(int);
	double tavan(double, double);
	double sin(double);
	double cos(double);
	double tan(double);
	double cot(double);
	double log(double, double);
	double ln(double);
	double sqrt(double, double);
};
